# 前端表现层与交互层 · 个人开发路线图(B)

> 范围:仅任务 2(用户界面)与任务 4(绘图与编辑)。
> 任务 3(元件库)、任务 5(文件功能)、任务 6(电路仿真)由其他同学负责,本路线图不涉及。
> 接口契约(阶段四使用):`SchematicModel`(原理图数据)与 `Simulator`(仿真 + 电平回调),签名见 docs/interfaces.md。

---

## 阶段一:静态骨架搭建

静态搭建各界面组件并完成布局,不含交互与真实数据。

- [x] 环境配置(CMake + wxWidgets)——已跑通
- [x] wxFrame 主窗口(含状态栏)
- [x] 菜单栏
- [x] 工具栏
- [x] wxTreeCtrl 树形元件库(挂载到布局)—— 可用 wxFormBuilder 拖出骨架再手改
- [x] wxPropertyGrid 属性表(挂载到布局)—— 需要时在 CMakeLists.txt 的 wx 组件里加 propgrid

## 阶段二:只读画布渲染

- [x] 画布容器(自定义 wxPanel / wxScrolledWindow,接入 OnPaint)
- [x] wxPaintDC 绘制网格背景
- [x] 绘制假元件(矩形 + 引脚占位)
- [x] 绘制假导线

## 阶段三:交互与布线核心

- [ ] Hit Testing 命中检测(元件 / 引脚 / 导线)
- [ ] 元件拖拽(鼠标按下 → 移动 → 抬起,更新位置)
- [ ] 导线生成(引脚到引脚的连线交互)
- [ ] 选中态与高亮反馈

## 阶段四:前后端大融合

- [ ] 移除假元件 / 假导线数据
- [ ] 接入 `SchematicModel`(读 data() → 渲染)
- [ ] 实现 `Simulator` 回调,刷新画布与属性表
- [ ] 从元件库放置真实元件实例并回写模型
