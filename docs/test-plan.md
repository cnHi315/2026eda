# 测试用例(手工核对)

> 状态:□草稿 ☑评审中 □稳定 | 负责人:全组 | 最后更新:2026-09-24(每次修改后更新这行)
> 每完成一项,把"实际"和"状态"填上。演示前必须全绿。

| 编号 | 步骤 | 预期 | 实际 | 状态 |
| --- | --- | --- | --- | --- |
| T-01 | 构建并启动程序 | 弹出标题为 Circuit Editor 的空白主窗口(菜单栏/工具栏/状态栏) | Shuai 已跑通 | ✅ |
| T-02 | 从元件库选与门,点击画布 | 画布出现与门符号,可拖动 | 阶段三已支持画布内选中/拖拽(见 T-09);"从元件库选类型后点击画布放置"仍需阶段四接 `addElement` | |
| T-03 | 从 SW1 引脚拖到 U1.A | 出现导线;数据模型中生成 net | 阶段三已能从引脚拖到引脚生成 Wire(画布假数据);net 生成待阶段四接 A 的 `addWire` | |
| T-04 | 保存为 demo.json 再打开 | 元件、位置、连线全部恢复 | | |
| T-05 | 导出网表 | 生成文本网表,包含元件和网络 | | |
| T-06 | 仿真:SW1=1,SW2=1 | LED 亮 | | |
| T-07 | 仿真:SW1=0,SW2=1 | LED 灭 | | |
| T-08 | 启动后核对画布渲染(阶段二,B) | 网格 + 4 个假元件(矩形 + 引脚占位)+ 3 条假导线正常显示 | 实跑截图核对通过:20px 网格、SW1/SW2/U1(AND)/LED1 四个符号、SW1→U1.A / SW2→U1.B / U1.Y→LED1.A 三条导线 | ✅ |
| T-09 | 阶段三交互核对:B(命中 / 选中 / 拖拽 / 连线) | 点选元件出现高亮;拖动元件位移并吸附栅格;引脚到引脚拖出橡皮筋生成导线;自环等非法连线被拒;Esc 取消可还原 | 机器化核对 12/12 通过:点 U1 出现蓝色选中框(1864 像素)、单击不产生位移、SW1 拖到 (180,200) 且原位已空、新增 SW1.Y→LED1.A 导线、自环被拒并给出原因、Esc 让 SW2 回到 (100,300) | ✅ |

### 补充：C 侧 ComponentLibrary 核对(任务 3)

> 方式:编译后在临时 main 或调试断点中调用,打印/查看返回值核对。

| 编号 | 步骤 | 预期 | 实际 | 状态 |
| --- | --- | --- | --- | --- |
| C-T-01 | 调 `types()` | 返回 5 个:AND / OR / NOT / SWITCH / LED | | |
| C-T-02 | 调 `displayName("AND")` 等五个 | 与门 / 或门 / 非门 / 开关 / LED | | |
| C-T-03 | 调 `pinTemplate("AND")` | 3 引脚:A/B 输入(-50,-10)(-50,10),Y 输出(50,0) | | |
| C-T-04 | 调 `pinTemplate("OR")` | 3 引脚,relPos 与 AND 对称 | | |
| C-T-05 | 调 `pinTemplate("NOT")` | 2 引脚:A(-50,0) 输入,Y(50,0) 输出 | | |
| C-T-06 | 调 `pinTemplate("SWITCH")` | 2 引脚:A(-50,0) 输入,Y(50,0) 输出 | | |
| C-T-07 | 调 `pinTemplate("LED")` | 2 引脚:A(-50,0) 输入,K(50,0) 输出 | | |
| C-T-08 | 调 `pinTemplate("UNKNOWN")` | 返回空 vector,程序不崩溃 | | |	
